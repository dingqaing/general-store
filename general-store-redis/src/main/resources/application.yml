
server:
  port: 8001

spring:
  application:
    name: general-store
  redis:
    host: "****"
    port: 6379
    jedis:
      pool:
        max-active: 1000
        max-wait: 10000
        max-idle: 200
        min-idle: 50
        time-between-eviction-runs: 100000
    client-type: jedis
  datasource:
    url: "****"
